<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %>/<%= user.age %>
      <%= link_to '詳細', user_path(user.id) %>
    </li>
  <% end %>
</ul>

<div>
  <%= button_to "登録", { action: "new" }, method: :get %>
</div>
